<h2>{{title}}</h2>

<h3 style="margin-top: 30px;">Lista de Productos (Home)</h3>

{{#if products}}
    <ul style="list-style: none; padding: 0;">
        {{#each products}}
            <li style="border: 1px solid #ddd; padding: 10px; margin-bottom: 5px;">
                <strong>{{this.title}}</strong> ({{this.category}})
                <br>
                Precio: ${{this.price}} | Stock: {{this.stock}}
            </li>
        {{/each}}
    </ul>
{{else}}
    <p>No hay productos disponibles.</p>
{{/if}}

<p style="margin-top: 20px;"><a href="/realtimeproducts">Ir a la Vista de Tiempo Real</a></p>